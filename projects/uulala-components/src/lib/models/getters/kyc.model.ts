export interface kycModel {
	frontId: string;
	backId: string;
	faceId: string;
	typeIdentification: string;
	documentId: string;
	id: string;
	kycid: string;
	status: number;
	isComplete: boolean;
}